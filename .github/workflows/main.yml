name: RSS to Discord Auto Post

on:
  schedule:
    - cron: '0 7 * * *'   # Runs every day at 07:00 UTC
  workflow_dispatch:       # Allows manual run from GitHub UI

jobs:
  rss-to-discord:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Run RSS to Discord script
        env:
          DISCORD_WEBHOOK_URL: ${{ https://discord.com/api/webhooks/1390610523581841488/4ug1f8hsmetljyFyqGT-vnHTxxi7q8itwy4g4hQXy7BogBwOl2WaoSU6rkaQF_wLSkMY }}
          RSS_URL: ${{ https://www.antaranews.com/rss/dunia.xml }}
        run: node rss-to-discord.js

      - name: Commit and push log updates
        run: |
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"
          git add log.txt log_number.txt
          git commit -m "auto-update log.txt (github actions)" || echo "No changes to commit"
          git push
